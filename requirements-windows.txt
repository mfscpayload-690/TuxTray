# TuxTray Windows-specific Requirements
# =====================================
# Core dependencies for Windows 10/11 compatibility

# Cross-platform dependencies (same as requirements.txt)
PySide6>=6.4.0
psutil>=5.9.0
Pillow>=9.0.0

# Windows-specific build dependencies
pyinstaller>=5.0.0
pywin32>=306; sys_platform == "win32"
pywin32-ctypes>=0.2.0; sys_platform == "win32"

# Optional Windows enhancements
# wmi>=1.5.1; sys_platform == "win32"  # Windows Management Instrumentation for enhanced system info
# python-dateutil>=2.8.0  # For timestamp handling in Windows

# Development dependencies for Windows build
setuptools>=65.0.0
wheel>=0.38.0

# UPX (optional, for smaller executables)
# upx-ucl  # Uncomment if you have UPX installed for compression
